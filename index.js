import { registerRootComponent } from 'expo';
import App from './App';

// registerRootComponent takes care of registering your App component as the root of the application.
// It handles both native and web platforms correctly.
registerRootComponent(App);